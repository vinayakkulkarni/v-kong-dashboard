import {
  app,
  components,
  css,
  experimental,
  nitro,
  modules,
  plugins,
  routeRules,
  runtimeConfig,
  typescript,
} from './config';

export default defineNuxtConfig({
  app,
  components,
  css,
  experimental,
  nitro,
  modules,
  plugins,
  routeRules,
  runtimeConfig,
  typescript,
});
